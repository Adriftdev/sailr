schema_version = "0.5.0"
name = "kind-signed"
log_level = "info"
domain = "kind.test"
default_replicas = 1
registry = "docker.io"

[deployment_policy]
required_approval = "signature"

[[service]]
name = "approved"
version = "test"
path = "kind/approved"
namespace = "default"
